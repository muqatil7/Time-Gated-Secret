<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>كل صفحات HTML</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/public/styles.css" />
</head>
<body class="bg-slate-900 min-h-screen antialiased p-0">
  <nav class="w-full py-3 px-6 bg-slate-800 border-b border-indigo-700 shadow flex items-center justify-between sticky top-0 z-10">
    <div>
      <a href="/html" class="text-indigo-300 px-3 py-2 hover:bg-slate-700 rounded transition text-lg font-bold">إنشاء صفحة جديدة</a>
    </div>
    <div>
      <span class="text-white text-lg font-semibold">كل صفحات HTML</span>
    </div>
  </nav>
  <main class="max-w-3xl mx-auto p-6">
    <h1 class="text-2xl font-bold mb-4 text-white">قائمة الصفحات المحفوظة</h1>
    <% if (pages.length === 0) { %>
      <div class="text-gray-200">لا توجد صفحات محفوظة.</div>
    <% } else { %>
      <ul class="space-y-3 mt-5">
        <% pages.forEach(function(p) { %>
        <li class="flex items-center gap-4 bg-slate-800 rounded px-4 py-3">
          <span class="flex-1 text-lg text-indigo-200 font-mono"><%= p %></span>
          <a href="/pages/<%= p %>" target="_blank" class="px-3 py-1 rounded bg-indigo-600 text-white hover:bg-indigo-800 transition">عرض</a>
          <a href="/pages/<%= p %>/edit" class="px-3 py-1 rounded bg-yellow-500 text-black hover:bg-yellow-400 transition">تعديل</a>
          <form method="POST" action="/pages/<%= p %>/delete" onsubmit="return confirm('هل أنت متأكد من حذف الصفحة؟')">
            <button type="submit" class="px-3 py-1 rounded bg-red-600 text-white hover:bg-red-800 transition">حذف</button>
          </form>
        </li>
        <% }) %>
      </ul>
    <% } %>
  </main>
</body>
</html>