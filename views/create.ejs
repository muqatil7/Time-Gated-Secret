<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>إنشاء صفحة - HTML Gallery</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/styles.css">
</head>
<body class="section">
  <div class="max-w-7xl mx-auto grid gap-6">
    <!-- Header -->
    <header class="glass rounded-2xl px-5 py-5 flex items-center justify-between">
      <a href="<%= selectedFolderId ? '/?folder=' + selectedFolderId : '/' %>" class="btn inline-flex items-center gap-2 bg-white text-violet-700 font-semibold py-2.5 px-4 rounded-xl shadow">← الرئيسية</a>
      <div class="text-right">
        <h1 class="h-title text-2xl sm:text-3xl font-extrabold text-white">✨ إنشاء صفحة HTML جديدة</h1>
        <p class="text-white/80 text-sm">أنشئ صفحة بمظهر حديث وجذاب</p>
      </div>
    </header>

    <!-- Form Card -->
    <section class="glass rounded-2xl p-6 grid gap-6">
      <form id="createForm" class="grid md:grid-cols-2 gap-6">
        <div class="grid gap-5">
          <div>
            <label class="block text-white font-semibold mb-2" for="name">📝 اسم الصفحة</label>
            <input type="text" id="name" name="name" required class="w-full px-4 py-3 border-2 border-white/30 rounded-lg focus:border-violet-500 focus:outline-none bg-white/80" placeholder="مثال: صفحتي الرائعة">
          </div>

          <div>
            <label class="block text-white font-semibold mb-2" for="folderId">📁 المجلد</label>
            <select id="folderId" name="folderId" class="w-full px-4 py-3 border-2 border-white/30 rounded-lg focus:border-violet-500 focus:outline-none bg-white/80">
              <option value="">المجلد الرئيسي</option>
              <% folders.forEach(folder => { %>
                <option value="<%= folder.id %>" <%= selectedFolderId === folder.id ? 'selected' : '' %>><%= folder.name %></option>
              <% }); %>
            </select>
            <p class="text-white/80 text-xs mt-1">اختر أين تريد حفظ الصفحة</p>
          </div>

          <div class="flex gap-3">
            <button type="button" onclick="previewPage()" class="btn flex-1 bg-blue-500 hover:bg-blue-600 text-white font-bold py-3 rounded-lg">👁️ معاينة</button>
            <button type="submit" class="btn flex-1 bg-emerald-500 hover:bg-emerald-600 text-white font-bold py-3 rounded-lg">✔️ حفظ</button>
          </div>
        </div>

        <div class="grid">
          <label class="block text-white font-semibold mb-2" for="htmlContent">💻 محتوى HTML</label>
          <textarea id="htmlContent" name="htmlContent"></textarea>
        </div>
      </form>
    </section>
  </div>

  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/theme/monokai.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/mode/htmlmixed/htmlmixed.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/mode/xml/xml.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/mode/javascript/javascript.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/mode/css/css.min.js"></script>

  <script>
    const editor = CodeMirror.fromTextArea(document.getElementById('htmlContent'), {
      mode:'htmlmixed', theme:'monokai', lineNumbers:true, lineWrapping:true, autoCloseTags:true, autoCloseBrackets:true,
      value:`<!DOCTYPE html>\n<html lang="ar">\n<head>\n  <meta charset=\"UTF-8\">\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n  <title>صفحتي</title>\n</head>\n<body>\n  <h1>مرحباً بك!</h1>\n</body>\n</html>`
    });

    function previewPage(){ const html=editor.getValue(); const w=window.open('','_blank'); w.document.write(html); w.document.close(); }

    document.getElementById('createForm').addEventListener('submit', async (e)=>{
      e.preventDefault();
      const name=document.getElementById('name').value; const htmlContent=editor.getValue(); const folderId=document.getElementById('folderId').value||null;
      if(!htmlContent.trim()){ alert('يجب إدخال محتوى HTML!'); return; }
      const res = await fetch('/api/pages',{ method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ name, htmlContent, folderId }) });
      if(res.ok){ alert('✅ تم حفظ الصفحة بنجاح!'); window.location.href = folderId? `/?folder=${folderId}` : '/'; }
      else{ const d=await res.json().catch(()=>({})); alert('❌ خطأ: '+(d.error||'فشل الحفظ')); }
    });
  </script>
</body>
</html>
