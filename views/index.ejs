<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HTML Gallery - معرض صفحات HTML</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
        }
        .btn {
            transition: all 0.3s ease;
        }
        .btn:hover {
            transform: scale(1.05);
        }
        .folder-card {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        }
    </style>
</head>
<body class="p-6">
    <div class="max-w-7xl mx-auto">
        <!-- Header -->
        <div class="text-center mb-12">
            <h1 class="text-5xl font-bold text-white mb-4 drop-shadow-lg">
                🌐 معرض صفحات HTML
            </h1>
            <p class="text-xl text-white/90 mb-8">أنشئ وعدّل واستعرض صفحات HTML بكل حرية</p>
            
            <!-- Navigation Breadcrumb -->
            <% if (currentFolder) { %>
                <div class="mb-6">
                    <a href="/" class="text-white hover:text-white/80 font-semibold">
                        🏠 الرئيسية
                    </a>
                    <span class="text-white mx-2">/</span>
                    <span class="text-white font-semibold">📁 <%= currentFolder.name %></span>
                </div>
            <% } %>
            
            <div class="flex flex-wrap gap-4 justify-center">
                <a href="/create<%= folderId ? '?folder=' + folderId : '' %>" class="btn inline-block bg-white text-purple-700 font-bold py-4 px-8 rounded-full shadow-lg hover:shadow-2xl">
                    ➕ إنشاء صفحة جديدة
                </a>
                <button onclick="showCreateFolderModal()" class="btn inline-block bg-yellow-400 text-purple-900 font-bold py-4 px-8 rounded-full shadow-lg hover:shadow-2xl">
                    📁 إنشاء مجلد جديد
                </button>
                <% if (currentFolder) { %>
                    <a href="<%= currentFolder.parentId ? '/?folder=' + currentFolder.parentId : '/' %>" class="btn inline-block bg-gray-200 text-gray-800 font-bold py-4 px-8 rounded-full shadow-lg hover:shadow-2xl">
                        ⬅️ العودة للأعلى
                    </a>
                <% } %>
            </div>
        </div>

        <!-- Folders Section -->
        <% if (folders.length > 0) { %>
            <div class="mb-8">
                <h2 class="text-3xl font-bold text-white mb-4">📁 المجلدات</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
                    <% folders.forEach(folder => { %>
                        <div class="card folder-card rounded-2xl shadow-xl p-6 text-white">
                            <div class="flex items-center justify-between mb-4">
                                <h3 class="text-2xl font-bold truncate" title="<%= folder.name %>">
                                    📁 <%= folder.name %>
                                </h3>
                            </div>
                            <div class="flex gap-2">
                                <a href="/?folder=<%= folder.id %>" class="btn flex-1 bg-white/20 hover:bg-white/30 text-white font-semibold py-2 px-3 rounded-lg text-center text-sm">
                                    فتح
                                </a>
                                <button onclick="renameFolder('<%= folder.id %>', '<%= folder.name %>')" class="btn flex-1 bg-white/20 hover:bg-white/30 text-white font-semibold py-2 px-3 rounded-lg text-sm">
                                    إعادة تسمية
                                </button>
                                <button onclick="deleteFolder('<%= folder.id %>', '<%= folder.name %>')" class="btn bg-red-500/80 hover:bg-red-600 text-white font-semibold py-2 px-3 rounded-lg text-sm">
                                    حذف
                                </button>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        <% } %>

        <!-- Pages Grid -->
        <% if (pages.length > 0) { %>
            <h2 class="text-3xl font-bold text-white mb-4">📄 الصفحات</h2>
        <% } %>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (pages.length === 0 && folders.length === 0) { %>
                <div class="col-span-full text-center py-16">
                    <div class="bg-white/20 backdrop-blur-lg rounded-2xl p-12 border border-white/30">
                        <p class="text-2xl text-white mb-4">📜 لا توجد صفحات أو مجلدات بعد</p>
                        <p class="text-white/80">ابدأ بإنشاء أول صفحة أو مجلد!</p>
                    </div>
                </div>
            <% } else if (pages.length === 0) { %>
                <div class="col-span-full text-center py-8">
                    <div class="bg-white/20 backdrop-blur-lg rounded-2xl p-8 border border-white/30">
                        <p class="text-xl text-white">📜 لا توجد صفحات في هذا المجلد</p>
                    </div>
                </div>
            <% } else { %>
                <% pages.forEach(page => { %>
                    <div class="card bg-white rounded-2xl shadow-xl overflow-hidden border-2 border-white/20">
                        <div class="bg-gradient-to-r from-purple-600 to-pink-600 p-6">
                            <h2 class="text-2xl font-bold text-white truncate" title="<%= page.name %>">
                                <%= page.name %>
                            </h2>
                            <p class="text-white/80 text-sm mt-2">
                                📅 <%= new Date(page.createdAt).toLocaleDateString('ar-EG', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                            </p>
                        </div>
                        
                        <div class="p-6">
                            <div class="flex flex-wrap gap-3">
                                <a href="/page/<%= page.id %>" target="_blank" 
                                   class="btn flex-1 bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 px-4 rounded-lg text-center shadow-md">
                                    👁️ عرض
                                </a>
                                <a href="/edit/<%= page.id %>" 
                                   class="btn flex-1 bg-green-500 hover:bg-green-600 text-white font-semibold py-3 px-4 rounded-lg text-center shadow-md">
                                    ✏️ تعديل
                                </a>
                                <button onclick="deletePage('<%= page.id %>', '<%= page.name %>')" 
                                        class="btn flex-1 bg-red-500 hover:bg-red-600 text-white font-semibold py-3 px-4 rounded-lg shadow-md">
                                    🗑️ حذف
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>

    <!-- Create Folder Modal -->
    <div id="createFolderModal" class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm flex items-center justify-center z-50">
        <div class="bg-white rounded-2xl p-8 max-w-md w-full mx-4 shadow-2xl">
            <h3 class="text-2xl font-bold text-gray-800 mb-4">📁 إنشاء مجلد جديد</h3>
            <input type="text" id="folderName" placeholder="اسم المجلد" 
                   class="w-full px-4 py-3 border-2 border-gray-300 rounded-lg focus:border-purple-500 focus:outline-none mb-4">
            <div class="flex gap-3">
                <button onclick="createFolder()" class="flex-1 bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-6 rounded-lg">
                    إنشاء
                </button>
                <button onclick="hideCreateFolderModal()" class="flex-1 bg-gray-300 hover:bg-gray-400 text-gray-800 font-bold py-3 px-6 rounded-lg">
                    إلغاء
                </button>
            </div>
        </div>
    </div>

    <script>
        const currentFolderId = '<%= folderId || "" %>';

        function showCreateFolderModal() {
            document.getElementById('createFolderModal').classList.remove('hidden');
            document.getElementById('folderName').focus();
        }

        function hideCreateFolderModal() {
            document.getElementById('createFolderModal').classList.add('hidden');
            document.getElementById('folderName').value = '';
        }

        async function createFolder() {
            const name = document.getElementById('folderName').value.trim();
            
            if (!name) {
                alert('يرجى إدخال اسم المجلد');
                return;
            }

            try {
                const response = await fetch('/api/folders', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ 
                        name, 
                        parentId: currentFolderId || null 
                    })
                });

                if (response.ok) {
                    alert('تم إنشاء المجلد بنجاح!');
                    location.reload();
                } else {
                    const data = await response.json();
                    alert('خطأ: ' + (data.error || 'فشل إنشاء المجلد'));
                }
            } catch (error) {
                console.error('خطأ:', error);
                alert('حدث خطأ أثناء إنشاء المجلد');
            }
        }

        async function deleteFolder(id, name) {
            if (!confirm(`هل أنت متأكد من حذف المجلد "${name}"؟\n\nتنبيه: لا يمكن حذف المجلد إذا كان يحتوي على صفحات أو مجلدات فرعية.`)) {
                return;
            }

            try {
                const response = await fetch(`/api/folders/${id}`, {
                    method: 'DELETE'
                });

                if (response.ok) {
                    alert('تم حذف المجلد بنجاح!');
                    location.reload();
                } else {
                    const data = await response.json();
                    alert('خطأ: ' + (data.error || 'فشل حذف المجلد'));
                }
            } catch (error) {
                console.error('خطأ:', error);
                alert('حدث خطأ أثناء حذف المجلد');
            }
        }

        async function renameFolder(id, oldName) {
            const newName = prompt('أدخل الاسم الجديد للمجلد:', oldName);
            
            if (!newName || newName === oldName) {
                return;
            }

            try {
                const response = await fetch(`/api/folders/${id}`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ name: newName })
                });

                if (response.ok) {
                    alert('تم تغيير اسم المجلد بنجاح!');
                    location.reload();
                } else {
                    const data = await response.json();
                    alert('خطأ: ' + (data.error || 'فشل تغيير الاسم'));
                }
            } catch (error) {
                console.error('خطأ:', error);
                alert('حدث خطأ أثناء تغيير الاسم');
            }
        }

        async function deletePage(id, name) {
            if (!confirm(`هل أنت متأكد من حذف الصفحة "${name}"؟`)) {
                return;
            }

            try {
                const response = await fetch(`/api/pages/${id}`, {
                    method: 'DELETE'
                });

                if (response.ok) {
                    alert('تم حذف الصفحة بنجاح!');
                    location.reload();
                } else {
                    alert('فشل حذف الصفحة');
                }
            } catch (error) {
                console.error('خطأ:', error);
                alert('حدث خطأ أثناء حذف الصفحة');
            }
        }

        // Enter key support for folder creation
        document.getElementById('folderName').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                createFolder();
            }
        });
    </script>
</body>
</html>