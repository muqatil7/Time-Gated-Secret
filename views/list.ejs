<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="/public/styles.css" />
  <meta http-equiv="Cache-Control" content="no-store" />
  <meta http-equiv="Pragma" content="no-cache" />
  <meta http-equiv="Expires" content="0" />
  <script defer src="/public/app.js"></script>
  </head>
<body class="min-h-screen antialiased">
  <div class="max-w-5xl mx-auto p-6">
    <header class="flex items-center justify-between" role="banner">
      <h1 class="text-3xl font-bold">All Secrets</h1>
      <a href="/new" class="inline-flex items-center rounded-md px-3 py-1.5 border border-slate-600 hover:bg-slate-800 focus:outline-none focus:ring-2 focus:ring-indigo-500">Create new</a>
    </header>

    <% if (typeof message !== 'undefined' && message) { %>
      <div class="mt-4 rounded-md border border-green-300 bg-green-900/20 p-3 text-green-200"><%= message %></div>
    <% } %>

    <main class="mt-6 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4" role="main" aria-label="Secrets list">
      <% if (!items || !items.length) { %>
        <div class="text-slate-400">No secrets yet.</div>
      <% } %>
      <% (items || []).forEach(function(item){ %>
        <a href="/s/<%= item.id %>" class="block card p-4 shadow-sm hover:shadow-md focus:outline-none focus:ring-2 focus:ring-indigo-500" aria-label="Secret <%= item.id %> <%= item.isVisible ? 'visible' : 'hidden' %>">
          <div class="flex items-center justify-between mb-2">
            <div class="font-semibold">#<%= item.id %></div>
            <% if (item.isVisible) { %>
              <span class="inline-flex items-center rounded px-2 py-0.5 text-green-300 text-xs bg-green-800/20">Visible</span>
            <% } else { %>
              <span class="inline-flex items-center rounded px-2 py-0.5 text-amber-300 text-xs bg-amber-800/20">Hidden</span>
            <% } %>
          </div>
          <div class="text-xs text-slate-400">TZ: <%= item.timezone %></div>
          <div class="text-xs text-slate-400 mb-2">Created: <%= item.createdAt %></div>
          <% if (item.preview) { %>
            <div class="text-sm truncate-2"><%= item.preview %></div>
          <% } else { %>
            <div class="text-sm text-slate-400 italic">Secret content hidden</div>
          <% } %>
        </a>
      <% }) %>
    </main>
  </div>
</body>
</html>


